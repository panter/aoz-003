- margin_r = 'margin-right: 5px;'

h3
  = "#{@volunteer.contact.title} " if @volunteer.contact.title.present?
  = "#{@volunteer.contact.first_name} #{@volunteer.contact.last_name}"

p= t('.please_confirm', client: @reminder.assignment.client)
p= link_to t_title(:index, Hour), volunteer_hours_url(@volunteer)
p= link_to t_title(:index, AssignmentJournal), assignment_assignment_journals_url(@reminder.assignment)
p= link_to t_title(:edit, Assignment), edit_assignment_url(@reminder.assignment)
